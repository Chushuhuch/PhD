\section{”слови€, необходимые дл€ выполнени€ неравенства (\ref{toprove})}

Ќеобходимые услови€ обсуждались нами в работе \cite{1dim}. ѕриведем здесь лишь формулировки.

\begin{thm}
{\bf 1} ≈сли неравенство $(\ref{toprove})$ выполн€етс€ дл€ некоторой $F \in \mathfrak{F}$
и произвольной кусочно линейной $u$, то вес $a$ четен по $y$,
то есть $a(x', y, v) \equiv a(x', -y, v)$.

{\bf 2} ≈сли неравенство $(\ref{toprove})$ выполн€етс€ дл€ произвольной $F \in \mathfrak{F}$
и произвольной кусочно линейной $u$, то вес $a$ удовлетвор€ет неравенству
\begin{equation}
\label{almostConcave}
a( x', s, v ) + a( x', t, v ) \ge a( x', 1 - t + s, v ), \qquad x \in \omega, -1 \le s \le t \le 1, v \in \Real_+.
\end{equation}
\end{thm}

\begin{rem}
≈сли функци€ $a$ неотрицательна, а также четна и вогнута по первому параметру, то она удовлетвор€ет условию $(\ref{almostConcave})$.
ƒействительно: дл€ любых $s$, $t$ и $u$ выполнено $a( 1, u ) - a( s, u ) \le a( t, u ) - a( -1 + t - s, u )$.
ј так как $a( 1, u ) \ge 0$, то
получаем $a( s, u ) + a( t, u ) \ge a( -1 + t - s, u ) = a( 1 - t + s, u )$.
ќбратное вообще говор€ неверно, то есть не вс€ка€ четна€
неотрицательна€ функци€, удовлетвор€юща€ $(\ref{almostConcave})$, вогнута.
\end{rem}
