\section{Избавление от требования кусочной монотонности $a$}

Получим теперь неравенство для произвольных функций $a$, но для пространства
функций $u \in W(-1, 1)$, постоянных в $\eps$-окрестности $\pm 1$.

Зафиксируем некоторое $\eps$ и некоторую функцию $u \in \W(-1, 1)$, постоянную
на $[-1,-1 + \eps] \cup [1 - \eps, 1]$. Предположим, что
$\int_{-1}^1 F(u(x), a(x) u'(x))dx < \infty$. Возьмем число $\theta < 1$ и число
$k \in \Nat$ так, чтобы $\theta a_k(x) \le a(x)$ при
$x \in [-1 + \eps, 1 - \eps]$. Здесь $a_k$ --- кусочно-линейное приближение
$a$, состоящее из $k$ отрезков, которое, в соответствии с леммой
(\ref{partialLinearConcave}), удовлетворяет неравенству
(\ref{almostConcaveSum}). Покажем, что для любого
$\theta$ найдется такое $k$. Для любого $i$ будет выполнено
$\theta a_k(-1 + \frac{2i}{k}) < a(-1 + \frac{2i}{k})$. Для любого
неотрицательного $z < \frac{2}{k}$ по неравенству (\ref{almostConcaveSum})
$a(-1 + z) + a(1 - \frac{2i}{k} - z) \ge a(1 - \frac{2i}{k})$, то есть
$a(-1 + \frac{2i}{k} + z) \ge a(-1 + \frac{2i}{k}) - a(-1 + z)$, так как $a$
четная. С другой стороны, по неравенству (\ref{almostConcaveSum})
$a_k(-1 + \frac{2i}{k} + z) \le a_k(-1 + \frac{2i}{k}) + a_k(-1 + z)$.
Тем самым, $a(-1 + \frac{2i}{k} + z) - \theta a_k(-1 + \frac{2i}{k} + z) \ge
(1 - \theta) a(-1 + \frac{2i}{k}) - a(-1 + z) - \theta a_k(-1 + z)$.
Но $a(-1 + z) + \theta a_k(-1 + z) \le
2 \max \{ a(-1 + \zeta): \zeta \in [0, \frac{2}{k}]\} \to 0$.
Оценим снизу $a(-1 + \frac{2i}{k})$. Поскольку нас интересует неравенство на
$[-1 + \eps, 1 - \eps]$, нам достаточно оценки для
$-1 + \frac{2i}{k} \in [-1 + \eps - \frac{2}{k}, 1 - \eps + \frac{2}{k}]$.
А начиная с некоторого $k$, достаточно получить оценку при
$-1 + \frac{2i}{k} \in [-1 + \frac{\eps}{2}, 1 - \frac{\eps}{2}]$.
$a(-1 + \frac{2i}{k}) \ge
\min \{ a(x): x \in [-1 + \frac{\eps}{2}, 1 - \frac{\eps}{2}] \}$,
что положительно и не зависит от $k$.
Значит, найдется $k(\eps, \theta)$ такое, что $\theta a_k(x) \le a(x)$ при
$x \in [-1 + \eps, 1 - \eps]$.

Функции $\theta a_k$ кусочно-монотонные и удовлетворяют
(\ref{almostConcaveSum}), поэтому
\begin{equation}
\label{inequlityForPartiallyLinear}
\int_{-1}^1 F(u(x), \theta a_k(x) u'(x)) dx \ge
\int_{-1}^1 F(u^*(x), \theta a_k(x) {u^*}'(x)) dx.
\end{equation}
Покажем, что 
\begin{align*}
\int_{-1}^1 F(u(x), \theta a_k(x) u'(x)) dx \to &
\int_{-1}^1 F(u(x), a(x) u'(x)) dx \\
\int_{-1}^1 F(u^*(x), \theta a_k(x) {u^*}'(x)) dx \to &
\int_{-1}^1 F(u^*(x), a(x) {u^*}'(x)) dx.
\end{align*}
В обоих случаях подынтегральная функция левой части поточечно сходится к
подынтегральной функции правой части. Кроме того, функция
$F(u(x), a(x) u'(x))$ является суммируемой мажорантой для
$F(u(x), \theta a_k(x) u'(x))$, потому что $\theta a_k(x) \le a(x)$ и $F$
монотонно возрастает по второму аргументу. Для
$F(u^*(x), \theta a_k(x) {u^*}'(x))$ суммируемой мажорантой будет
$F(u^*(x), a(x) {u^*}'(x))$. Мажорантой она будет по тем же причинам.
Если она не суммируема, то $\int_{-1}^1 F(u^*(x), a(x) {u^*}'(x)) dx = \infty$,
что противоречит (\ref{inequlityForPartiallyLinear}) (!!!ФУФЛО).

Возьмем теперь произвольную $u \in \W(-1,1)$ и $u_\eps(x) = u(x)$ при
$x \in [-1 + \eps, 1 - \eps]$ и постоянную при остальных $x$. Тогда
$u_\eps \to u$ в $\W(-1,1)$ и $I(u_\eps) \to I(u)$. Поясним последнее.
$u_\eps \to u$ поточечно. Кроме того,
$I(u_\eps)=\eps F(u(-1 + \eps), 0) +
\int_{-1+\eps}^{1 - \eps} F(u(x), a(x) u'(x))dx + \eps F(u(1 - \eps), 0) \le
I(u) + 2\eps \max\{F(u(x), 0): x \in [-1,1]\} < \infty$. Тогда по лемме
(\ref{uplift}) получаем
\begin{prop}
Если функция $a$ непрерывна, не зависит от $u$ и удовлетворяет условию
(\ref{almostConcaveSum}), то для любой $u \in \W(-1,1)$ $I(u^*) \le I(u)$.
\end{prop}
